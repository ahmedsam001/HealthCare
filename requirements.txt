# Healthcare Real-Time Risk Monitoring Platform
# Pinned to match the verified working environment.

# Core data
pandas>=2.0
numpy>=1.24

# Snowflake
snowflake-connector-python[pandas]>=3.5.0

# Kafka
kafka-python>=2.0.2

# ML inference (must match training environment)
scikit-learn==1.6.1
xgboost>=2.0
joblib>=1.3

# Streamlit UI
streamlit>=1.35

# PySpark (Spark Structured Streaming)
# NOTE: The venv uses PySpark 3.5.6 with Spark 3.5.
# The notebooks reference PySpark 4.x — that is the Colab/training environment.
# The realtime pipeline runs on PySpark 3.5.6.
pyspark==3.5.6
